server:
  port: 8085
spring:
  application:
    name: genai-service
genai:
  api-key: ${GEMINI}

cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    region:
      static: ap-southeast-2

aws:
  s3:
    bucket: ${BUCKET}